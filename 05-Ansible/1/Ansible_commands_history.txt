## ANSIBLE_History_Commands ##
 -ansible --version > ansible_version.txt
 -ansible-doc -l | head -30 >> ansible_version.txt
 -which ansible
 -ansible --version
 -ansible-config dump | head -30
 -ansible localhost -m ping >> connection_test.txt
 -ansible-inventory --list >> connection_test.txt
 -ansible all --list-hosts >> connection_test.txt
 -ansible-inventory --host localhost >> connection_test.txt
 -ansible-inventory -i inventory --list >> inventory_operations.txt
 -ansible-inventory -i inventory --yaml >> inventory_operations.txt
 -ansible-inventory -i inventory --host --host web1 >> inventory_operations.txt
 -ansible-inventory -i inventory --list web1 >> inventory_operations.txt
 -ansible all -i inventory --hosts >> inventory_operations.txt
 -ansible all -i inventory --list >> inventory_operations.txt
 -ansible production -i inventory --hosts >> inventory_operations.txt
 -ansible production -i inventory --list >> inventory_operations.txt
 -ansible-inventory -i inventory --host web1 >> inventory_operations.txt
 -ansible all -i inventory -m ping >> inventory_operations.txt
 -ansible localhost -m file -a "path=/tmp/ansible_idempotency state=directory" >> idempotency_demo.txt
 -ansible localhost -m file -a "path=/tmp/ansible_idempotency state=directory" >> idempotency_demo.txt
 -ansible localhost -m file -a "path=/tmp/ansible_idempotency state=directory" >> idempotency_demo.txt
 -ansible-doc -l | head -50 >> modules_doc.txt
 -ansible-doc apt | head -100 >> modules_doc.txt
 -ansible-doc file | head -100 >> modules_doc.txt
 -ansible-doc copy | head -100 >> modules_doc.txt
 -ansible-doc -t module ping | grep -A 20 "EXAMPLES" >> modules_doc.txt
 -ansible localhost -m setup >> facts_demo.txt
 -ansible localhost -m setup -a "filter=ansible_distribution*" >> facts_demo.txt
 -ansible localhost -m setup -a "filter=ansible_default_ipv4" >> facts_demo.txt
 -ansible localhost -m setup -a "filter=ansible_memtotal_mb" >> facts_demo.txt
 -ansible localhost -m setup > facts_all.json
 -ansible-config dump >> config_demo.txt
 -ansible-config dump | grep inventory >> config_demo.txt
 -ansible-config dump | grep -i ssh >> config_demo.txt
 -ansible-config dump | grep become >> config_demo.txt
 -ansible localhost -m ping >> adhoc_output.txt
 -ansible localhost -m setup -a "filter=ansible_distribution*" >>adhoc_output.txt
 -ansible localhost -m file -a "path=/tmp/ansible_test state=directorymode=0755" >> adhoc_output.txt
 -ansible localhost -m stat -a "path=/tmp/ansible_test" >> adhoc_output.txt
 -ansible localhost -m command -a "uptime" >> adhoc_output.txt
 -ansible localhost -m shell -a "echo $HOME && whoami" >> adhoc_output.txt
 -ansible localhost -m copy -a "src=/tmp/test_source.txt dest=/tmp/test_dest.txt" >> adhoc_output.txt
 -ansible localhost -m get_url -a "url=https://www.google.com dest=/tmp/google.html" >> adhoc_output.txt
 -ansible localhost -m apt -a "name=curl state=present update_cache=yes" --become >> adhoc_output.txt 2>&1 || echo "Skipped no sudo access" >> adhoc_output.txt
 -
